# ------------------------------------------------------
# 環境変数など (秘密情報が含まれる場合があるため)
# ------------------------------------------------------
.env


# ---------------------------------------------------------------------
# 必須の除外設定 (依存関係とビルド成果物)
# ---------------------------------------------------------------------
node_modules/
dist/
out/
build/

# ---------------------------------------------------------------------
# VS Code 拡張機能 関連
# ---------------------------------------------------------------------
# パッケージ化された拡張機能ファイル
*.vsix

# テスト実行時に生成される一時的な設定フォルダ
.vscode-test/

# ---------------------------------------------------------------------
# エディタ設定 (ご要望により除外)
# ---------------------------------------------------------------------
# チーム開発で launch.json (デバッグ設定) を共有したい場合は
# .vscode/ をコメントアウトし、.vscode/settings.json だけ除外することもありますが、
# 個人開発や設定をリセットしたい場合はフォルダごと除外でOKです。
.vscode/

# ---------------------------------------------------------------------
# システム・ログファイル
# ---------------------------------------------------------------------
.DS_Store
Thumbs.db
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

.history
# ---------------------------------------------------------------------
# 注意点: wasm フォルダについて
# ---------------------------------------------------------------------
# 現在、wasmフォルダの中身は手動でコピー運用しているため、
# Gitに含める（除外しない）設定にしています。
# 将来的に npm script で自動コピーするようにした場合は
# 以下のアメンとアウトを外して除外対象にしてください。
# wasm/